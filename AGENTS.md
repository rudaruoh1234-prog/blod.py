# AGENTS.md

## Cursor Cloud specific instructions

This repository (`blod.py`) is currently an empty/scaffold project with only a `README.md`. There are no source files, dependencies, services, tests, or build steps.

- **Language:** The repo name suggests Python; Python 3.12 is available in the environment.
- **No dependencies** to install — no `requirements.txt`, `pyproject.toml`, `setup.py`, or similar.
- **No services** to start — no application code exists yet.
- **No tests** to run — no test files or test framework configured.
- **No lint/build** steps — no configuration for linters or build tools.

When source code and dependency files are added, update this section and the VM update script accordingly.
